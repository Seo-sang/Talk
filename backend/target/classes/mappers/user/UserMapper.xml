<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.double.mvcboard.mappers.user.UserMapper">
    <insert id="register">
        INSERT INTO tbl_user (
            email
            , password
            , nickname
            , statusmsg
        ) VALUES (
            #{email}
            , #{password}
            , #{nickname}
            , #{userEmail}
        )
    </insert>
</mapper>